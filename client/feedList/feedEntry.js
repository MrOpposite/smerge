_loadFeedEntry = function() {
  FeedEntry = function(extId, text, thumbUrl, poster, postedTime, extra) {
    this.extId      = extId;
    this.text       = text;
    this.thumbUrl   = thumbUrl;
    this.poster     = poster;
    this.postedTime = postedTime;
    this.extra      = extra;
  }
}
